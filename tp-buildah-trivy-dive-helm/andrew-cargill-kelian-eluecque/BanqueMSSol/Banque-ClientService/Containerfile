# --- Image Runtime (Uniquement) ---
FROM docker.io/library/eclipse-temurin:17-jre
WORKDIR /app

# Remédiation des failles OS sur l'image finale (Crucial pour Trivy)
USER root
RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Création de l'utilisateur non-root pour la sécurité
RUN groupadd -r miage && useradd -r -g miage miage

# On copie DIRECTEMENT le .jar généré par ton script Bash
COPY target/banque-clientservice-7.0.jar app.jar
RUN chown miage:miage app.jar

# Basculement vers l'utilisateur limité
USER miage

# Le port utilisé par le ClientService (vérifie si c'est bien 8080 ou 10011)
EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]